subprojects {
    group 'de.vmpclan'
    version '1.0-SNAPSHOT'

    repositories {
        mavenLocal()
        mavenCentral() // maven { url: 'http://jcenter.bintray.com' }
    }

    apply plugin: 'java'

    dependencies {
    }
}
